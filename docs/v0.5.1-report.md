# NIS Hub v0.5.1 report

## Baseline
- Base: merged v0.5 main `9d912674ab7fe8759a9d3e6b7b79675f177c701e`.
- Branch: `codex/v0.5.1-reader-ai-books`.
- All 11 package files, AGENTS.md and relevant installed Next.js 16 guidance read.
- Baseline: 72 tests (actual private local PDF supplied), typecheck, lint and build passed.
- Improved Telegram summaries are already merged and preserved.
- Owner clarified that the intended users are adults (18+). Gemini launch requires the operator to enforce that audience and review current provider terms.

## AI Study repair — 2026-09-16

Continued the owner's current `5977ebd` v0.5.1 state, preserving the existing provider-schema and diagnostic edits.

Confirmed causes, using synthetic non-private text only:
- The configured `gemini-2.5-flash` returned HTTP 404: unavailable to new API users. The provider recommended `gemini-3.6-flash`.
- The replacement model accepted a minimal request but rejected the bounded nested response schema with HTTP 400.
- A smaller structural schema plus the unchanged strict local response/citation validation succeeded. The final unmodified application provider returned a validated summary in about 5.9 seconds.

Changes:
- Only `GEMINI_MODEL` changed in ignored local `.env.local`; no keys changed or exposed. The deployment setting must be updated separately.
- Preserved server-side page/character limits, exact source quotation checks, cache signatures, per-user daily quota and failed-attempt accounting. No automatic provider retries or model fallback.
- Added localized configuration, timeout, provider-quota and in-flight messages.
- Retained diagnostic stages but allowlisted their payload; no raw errors, source text, user/book IDs, credentials or signed URLs are logged. Diagnostics are development-only.
- Repaired an unclosed callback in the existing AI test and updated legal-draft assertions for the already-added Gemini/adult sections.
- Added provider compatibility, actionable-error and safe-logging regression tests; setup and recovery are in `docs/gemini-ai-setup.md`.

No Supabase migration, Storage, RLS, book metadata, Reader rendering or authorization changes were needed for this repair. No live Supabase data was changed. Real provider diagnostics used small synthetic source text, never private textbook content.

Verification: focused AI tests, typecheck, lint and build passed. The final full-suite rerun is recorded below after completion.

Remaining real-session check: restart/reload the local app if necessary, generate a summary for actual pages 7–9, check the displayed source/quotes and repeat for a cache hit. The screenshot's exact private source is not accessible to this test session. Broader v0.5.1 manual release checks are not claimed complete by this repair.
