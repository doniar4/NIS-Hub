import type {ReactNode} from "react";
/** Entrance motion lives in styles/motion.css and honors reduced motion. */
export function HomeMotion({children}:{children:ReactNode}){return <div className="study-dashboard">{children}</div>;}
